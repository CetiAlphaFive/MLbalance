% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastcpt.plot.R
\name{plot.fastcpt}
\alias{plot.fastcpt}
\title{Plot fastcpt Results}
\usage{
\method{plot}{fastcpt}(x, breaks = 25, ...)
}
\arguments{
\item{x}{A fastcpt result object as returned by \code{\link{fastcpt}}.}

\item{breaks}{Number of breaks for the histogram. Default is 25.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot object displaying the null distribution histogram with the observed test statistic.
}
\description{
Creates a visualization of the Classification Permutation Test results. Displays the permuted null distribution as a histogram with the observed test statistic overlaid as a vertical line. The plot indicates whether the test passed (p > 0.05) or failed.
}
\examples{
\dontrun{
# Generate example data
n <- 200
p <- 10
Z <- matrix(rnorm(n * p), n, p)
T <- rep(c(1, 2), each = n/2)

# Run fast classification permutation test
result <- fastcpt(Z, T, class.methods = "forest", perm.N = 100)

# Plot the results
plot(result)
}

}
